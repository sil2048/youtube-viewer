// import { select{{ properCase name }}Domain } from '../services/selectors';

describe('select{{ properCase name }}Domain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
