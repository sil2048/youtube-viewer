/**
 * Test apis
 */

// import {{ camelCase name }}Api from '../services/api';

describe('{{ camelCase name }}Api Api', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
